import { PdfScrollDirective } from './pdf-scroll.directive';

describe('PdfScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new PdfScrollDirective();
    expect(directive).toBeTruthy();
  });
});
